#include "tissue.hpp"

Tissue::Tissue(int numAct, int numQuie){
  m_numAct = numAct;
  m_numQuie = numQuie;
}

int Tissue:updateTissue(){
  for(int i(0); i < m_cells.size(); i++){
    (m_cells.at(i)).updateCell(newCell, killCell);
    if(killCell)
